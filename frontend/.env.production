# Frontend production environment
# Used during `npm run build` — these values are baked into the static bundle.

# API base URL — points to the nginx-proxied API on the live domain
VITE_API_URL=https://play.lazplay.tech/api/v1
